apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: flappybird
  name: flappy-deploy
spec:
  selector:
    matchLabels:
      app: flappybird
  template:
    metadata:
      labels:
        app: flappybird
    spec:
      containers:
      - name: cont1
        image: anitalluri00/flappybird
        ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: abc
spec:
  type: LoadBalancer
  selector:
    app: train
  ports:
    - port: 80
      targetPort: 80
